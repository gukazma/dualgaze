# Copy to .env.local and fill in your own keys.
# Restart `pnpm dev` after editing — Vite only reads env at startup.

# AMap (高德地图) Web JS API key — used by LocationSearchTab's "地名" tab.
#
# 申请步骤：
#   1. 打开 https://console.amap.com → 注册账号 + 实名认证（个人/企业均可）
#   2. 应用管理 → 我的应用 → 创建新应用 → 添加 Key
#   3. 服务平台选「Web 端 (JS API)」⚠️ 必须是这个
#   4. 域名白名单写：localhost, 127.0.0.1（生产再加上线域名）
#   5. 提交后复制 key + 安全密钥 jscode（2021-12 后强制要 jscode）
#   6. 填到下面 → 重启 pnpm dev（Vite 只在启动读 env）
#
# 没配也不影响其它功能；只是"地名"搜索会显示"未配置 key"提示。
VITE_AMAP_KEY=
VITE_AMAP_JSCODE=
