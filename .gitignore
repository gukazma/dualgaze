# Node
node_modules/
*.log

# Build
dist/
*.tsbuildinfo

# tsc 误 emit 的 .js 残渣（tsconfig 已加 noEmit:true，留兜底）
src/**/*.js
vite.config.js

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Claude Code session state
.claude/

# Env
.env
.env.local
.env.*.local

# Playwright (调试用，不入库)
.playwright-mcp/
*.png

# Real-world point cloud data —— Bavaria 工程 P06_PNTS 输出，11.9 MB 一并入库做 demo
# （想换成自己的数据时把 tileset.json + Tile/ 替换掉即可）
